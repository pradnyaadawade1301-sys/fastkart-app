// lib/constants/app_strings.dart

class AppStrings {
  AppStrings._();

  // ── App ───────────────────────────────────────────────────────────────────
  static const appName     = 'FastKart';
  static const appTagline  = 'Food, Fast.';

  // ── Login / OTP ───────────────────────────────────────────────────────────
  static const loginTitle    = 'Welcome back';
  static const loginSub      = 'Sign in to continue';
  static const loginContinue = 'Continue';
  static const loginOtp      = 'Enter OTP';
  static const loginOtpSub   = 'OTP sent to';
  static const loginVerify   = 'Verify';

  // ── Profile ───────────────────────────────────────────────────────────────
  static const profileAddresses = 'Saved Addresses';
  static const profilePayments  = 'Payment Methods';
  static const profileOffers    = 'Offers & Coupons';
  static const profileSupport   = 'Help & Support';
  static const profileAbout     = 'About';
  static const profileLogout    = 'Logout';

  // ── Address ───────────────────────────────────────────────────────────────
  static const addressAdd   = 'Add New Address';
  static const addressHome  = 'Home';
  static const addressWork  = 'Work';
  static const addressOther = 'Other';
}